"""TurnContext model — per-turn ownership + target resolution state. Carries: thread_owner (general | rfq_id), page_default_rfq, current_turn_target(s) (single | comparison_set | cross_target), unresolved_clarification_target. Anchors per-target labelling (I7, I8) and the target_isolation guardrail at the type level; prevents cross-RFQ field leakage by construction rather than by post-hoc check."""
